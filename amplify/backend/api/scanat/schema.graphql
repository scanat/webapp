type Notifications @model {
  id: ID!
  topic: String
  description: String
  status: Boolean
  subscriberId: String
  expiry: String
  image: String
}

type UserTable @model {
  id: ID!
  saved: [String!]
  orders: [String!]
}